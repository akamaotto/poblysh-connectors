# Example environment configuration for the Connectors service
# Copy this file to `.env` or `.env.local` and adjust values as needed.

POBLYSH_PROFILE=local
POBLYSH_API_BIND_ADDR=0.0.0.0:8080
POBLYSH_LOG_LEVEL=info
POBLYSH_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/poblysh

# Operator authentication (choose either a single token or a comma-separated list)
POBLYSH_OPERATOR_TOKEN=local-dev-token
# POBLYSH_OPERATOR_TOKENS=token-a,token-b,token-c

# 32-byte base64 key used for AES-256-GCM token encryption.
# Generate a strong value with: openssl rand -base64 32
POBLYSH_CRYPTO_KEY=E5p9Q9j5hQ0yQT18lHG4f1yGmUDw53VJr4KUUHQVIsM=
